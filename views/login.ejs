<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link rel="stylesheet" href="/stylesheets/login.css">
</head>
<body>
    <div class="container">
        <div class="login-container">
            <form action = "/login" method = "post">
            <h2>Login</h2>
            <form id="loginForm">
                <div class="input-group">
                    <input type="email"  minlength="10" maxlength="25" id="email" name = "email" required>
                    <label for="email">Email</label>
                </div>
                <div class="input-group">
                    <input type="password" minlength="4" maxlength="15" id="password" name = "password" required>
                    <label for="password">Password</label>
                </div>
                <button type="submit">Login</button>
                <p class="signup-link">Don't have an account? <a href="/register">Sign up</a></p>
            </form>
            </form>
        </div>
    </div>
    <script src="script.js"></script>
</body>
</html>